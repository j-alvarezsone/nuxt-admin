# Copy this to .env and paste your full database connection string as the value for DATABASE_URL.
# For managed Postgres (Neon/Heroku/etc.) use `sslmode=verify-full` to verify server certificates.
# For local development you can use `sslmode=disable` but do NOT use that in production.
# If your password or other parts contain special characters (@, :, /), URL-encode them (e.g. @ → %40).
# Examples:
#  Managed DB: DATABASE_URL="postgresql://user:<PASSWORD>@host:5432/dbname?sslmode=verify-full"
#  Local dev:  DATABASE_URL="postgresql://user:<PASSWORD>@localhost:5432/dbname?sslmode=disable"
DATABASE_URL="<YOUR_DATABASE_CONNECTION_STRING>"
